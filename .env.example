# Django
DEBUG=True
SECRET_KEY=change-me

# PostgreSQL (default database backend)
DB_NAME=libra_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_CONN_MAX_AGE=60

# Optional: use SQLite for local fallback
# USE_SQLITE=True
